# 變更與進度

## 目前專案所處階段

若用一句話描述：

> **本專案已進入可由 CLI 直接操作、具備 chat mode 初版、skill registry v2 基礎結構、analyze 第二輪降噪，以及 session / diff / rollback 操作面的本地端 coding agent 建設期。**

---

## 目前已完成的項目

### CLI 主線
- `skills`
- `plan`
- `run`
- `session list`
- `session resume`
- `diff`
- `rollback`
- `chat` 初版

### skill system
- `analyze_repo`
- `safe_edit`
- registry v2 基礎 metadata
- capability / alias / hint routing 初版

### analyze
- 第二輪降噪
- `important_modules`
- 更聚焦的 repo overview

### 品質狀態
- `uv run pytest`
- **40 passed**

---

## 目前仍存在的限制

### chat 已進入 v2 初版
目前 chat mode 已具備：
- session-aware execution
- `/apply`
- `/status`
- `/session list`
- `/session resume <id>`
- `/diff [session_id]`
- `/rollback [session_id]`

仍待補強：
- 更完整的 multi-turn memory
- richer trace / explanation
- 更細的 confirmation checkpoints

### registry 還是基礎版
目前尚未做到：
- manifest
- plugin-like loading
- chaining
- fallback policy engine
- richer routing strategy

### analyze 還可再更好
目前仍是結構化 summary，未來可升級為 narrative summary。

---

## 下一步最值得做

1. richer agent loop
2. skill registry v3
3. analyze narrative summary
4. chat memory / history\n