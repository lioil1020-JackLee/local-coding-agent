# 04_開發流程_協作與進度

## 這份文件的定位

本文件整合：

- 開發流程
- 協作原則
- 文件維護規則
- 進度描述方式

目的不是保留所有歷史片段，而是確保文件與代碼保持一致。

---

## 目前實際開發流程

1. 在本地 repo 修改
2. 使用 AI 輔助分析與整理
3. 回 repo 驗證
4. 執行 CLI 與測試
5. 再更新文件

> **以實際代碼與執行結果為準**

---

## CLI / chat 更新原則

### CLI
以 `repo_guardian_mcp/cli.py` 為準：

- skills
- plan
- run
- chat
- session list
- session resume
- diff
- rollback

### chat
以 `cli_chat_service.py` 為準：

- /help
- /skills
- /plan
- /run
- /apply
- /status
- /session list
- /session resume
- /diff
- /rollback
- /exit

---

## Skill system（已修正，與代碼對齊）

### 目前已具備能力

從 `skills.py` 可確認：

- SkillMetadata
- SkillManifest
- from_dict()
- from_json_file()
- register_manifest()
- register_manifest_file()

metadata 包含：

- aliases
- examples
- routing_hints
- requires_session
- requires_validation
- priority
- enabled
- can_chain_to
- fallback_skills
- manifest_path

---

### 正確描述（取代舊錯誤說法）

❌ 舊說法（錯誤）
- 尚未做到 manifest
- 尚未做到 plugin-like loading

✅ 正確說法

> **目前已具備 manifest 基礎能力（SkillManifest、from_json_file、register_manifest 等），並支援 metadata routing、chaining、fallback 等機制；但尚未成熟為完整的 plugin-like loading 或 plugin ecosystem。**

---

## repo_guardian_agent 說明

- 存在於 repo 中
- 但不是 pyproject 主線
- CLI 與封裝主體在 repo_guardian_mcp

---

## 測試與進度

不要寫死：

- pytest passed 數字
- 固定測試清單

應寫：

- 以 tests/ 現況為準
- 以 uv run pytest 為準

---

## 文件維護 checklist

更新文件前確認：

1. pyproject.toml
2. cli.py
3. cli_chat_service.py
4. skills.py
5. tests/

---

## 結論

> 文件必須反映當前代碼，而不是歷史狀態。
